# Tier 9: Natural convection

Change rayleigh and prandtl. The left hot wall drives upward flow; the right cold wall drives downward flow.

Run from the package root:

```bash
python -m cavity_cases --case cases/tier-9/case.json --animate
```

All inputs are dimensionless. See the root README for equations, scales, boundaries, and limitations.
