# Tier 6: Moving-lid cavity

Change reynolds and n. Velocity and pressure are solved together.

Run from the package root:

```bash
python -m cavity_cases --case cases/tier-6/case.json --animate
```

All inputs are dimensionless. See the root README for equations, scales, boundaries, and limitations.
