# Tier 8: Heated moving-lid cavity

Change richardson to compare buoyancy with lid-driven circulation. The fluid transports temperature.

Run from the package root:

```bash
python -m cavity_cases --case cases/tier-8/case.json --animate
```

All inputs are dimensionless. See the root README for equations, scales, boundaries, and limitations.
