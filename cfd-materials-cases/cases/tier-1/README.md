# Tier 1: Channel flow

Change mu, height, and pressure_gradient. Compare numerical velocity with the analytical profile. Run --studies to check second-order refinement.

From the package root:

```bash
python -m cfd_cases --case cases/tier-1/case.json
```

See the root README for governing equations, units, boundary conditions, and assumptions.
