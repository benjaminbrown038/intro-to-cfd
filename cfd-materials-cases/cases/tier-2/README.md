# Tier 2: Duct cross-section

Change shape to rectangle, ellipse, or insert. Flow is perpendicular to the plot. The insert runs along the entire duct. Refine nx and ny to investigate staircase geometry error.

From the package root:

```bash
python -m cfd_cases --case cases/tier-2/case.json
```

See the root README for governing equations, units, boundary conditions, and assumptions.
